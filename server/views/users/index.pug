extends ../layouts/application.pug

block content
  table.table.table-striped
    thead
      tr
        th= t('views.user.id')
        th= t('views.user.name')
        th= t('views.user.email')
        th= t('views.user.createdAt')
        th
    tbody
      each user in users
        tr
          td= user.id
          td= user.name
          td= user.email
          td= formatDate(user.createdAt)
          td
            a(href=route('editUser', { id: user.id }))= t('views.user.edit.submit')
            +buttonTo(route('user', { id: user.id }), 'delete')(class="btn btn-link" value=t('views.user.delete.submit'))
